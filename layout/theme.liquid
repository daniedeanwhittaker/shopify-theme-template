<html>
  <head>
    <title>{{ page_title }}</title>
    {{ content_for_header }}
    {{ 'styles.css' | asset_url | stylesheet_tag }}
    {{ 'https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css' | stylesheet_tag }}
  </head>
  <body>
    {% section 'header-top' %}

    <!-- This is hard coded file {/% section 'header' %/} -->
    <!-- doing it this way includes the json and creates the ability to edit the file within shopify -->
    <!-- Shopify can then write to header-group.json -->
    {% sections 'header-group' %}

    {{ content_for_layout }}

    {% section 'footer' %}
  </body>
</html>
