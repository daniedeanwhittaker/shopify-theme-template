<html>
    <head>
        {{ page_title }}
        {{ content_for_header }}
        {{ 'styles.css' | asset_url | stylesheet_tag }}
    </head>
    <body>
        {% section "header" %}
        {{ content_for_layout }}
    </body>
</html>